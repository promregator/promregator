promregator:
  metrics:
    internal: true
  
  authenticator:
    type: none
    
  endpoint:
#    auth: BASIC
    
  discovery:
    hostname: localhost
  
  targets:
    - orgName: org
      spaceName: dev
      applicationName: testapp
      path: /metrics

    - orgName: org
      spaceName: dev
      applicationName: testapp2
      path: /metrics

cf:
  api_host: api.host
  username: username
  
  cache:
    timeout:
      org: 60
      space: 30
      application: 10
